# Data never goes in git. The database is ~1 GB and the CSVs are ~700 MB, and
# both are regenerated by build-db.py from the pipeline's output.
*.sqlite3
*.sqlite3-*
*.csv
seed/
__pycache__/
*.bak
.railway/
